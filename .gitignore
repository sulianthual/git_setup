
## GitIgnore (order matters!)

##################################################
### KEEP ONLY SPECIFIC FILETYPES (ALL DIRECTORIES)
## IF WANT TO KEEP EVERYTHING, COMMENT THIS SECTION OR DELETE .gitignore

# Ignore everything (recursive)
*
# But keep functonial files
!.gitattributes
!.gitignore
!.gitkeep
!*.md

# Keep specific file types (CUSTOMIZE HERE)
!*.txt
!*.sh
!*.py
!*.ipynb
!*.pdf

# Be Sure to browse subdirectories (recursive)
!*/

##################################################


## Some already pushed files not removed by later gitignore
## to remove them manually:
## git rm -r --cashed
## then regular add/commit/push
